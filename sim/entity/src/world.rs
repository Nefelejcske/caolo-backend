use crate::collections::handle_table::HandleTable;

pub struct World {
    entities: HandleTable,
}

impl World {
    pub fn new(max_entities: u32) -> Self {
        todo!()
        // Self {
        // }
    }
}
